# Paywall verification — 2026-09-15

Status: NOT signed off. No Apple purchase completed during these checks.

Production investigation: user reports public App Store build 0.1.49 (52), Germany storefront, iOS 27 beta. Archived binary inspection confirms the bundle ID, all subscription product IDs, and expected RevenueCat public key. All three Apple subscriptions are approved and available worldwide. Agreement became effective September 15. Apple Support case **102964170126** submitted with authorization to investigate production catalog activation. No connected physical device was available for StoreKit logs. Activation propagation and beta-specific behavior remain unconfirmed hypotheses.

## Passed

- Live RevenueCat offerings request using the app's configured public SDK key: HTTP 200, default offering contains mogging.pro.weekly, mogging.pro.monthly, mogging.pro.yearly.
- Production /api/payments/upgrades: authenticated catalog and pre-purchase synchronization return HTTP 200 for a disposable test account. Backend RevenueCat credentials work.
- An invented transaction is rejected with HTTP 409 and creates no credits. Test account/session removed afterward.
- Isolated database tests: single purchase +1, pack +3, concurrent duplicate synchronization, no duplicate credit across accounts, concurrent spending cannot overdraw, exhausted credits rejected, restores preserve spent balances, repeat pack adds +3, refunds remove remaining credit and stale synchronization does not restore refunded credit, active/expired Pro.
- Mobile payment-response tests: 3 passed. RevenueCat attribution lifecycle tests: 10 passed.
- Mobile TypeScript check passed.
- Apple Paid Apps Agreement, bank account, and tax forms verified Active in App Store Connect.
- RevenueCat Apple in-app purchase key and App Store Connect API key revalidated successfully. The `pro` entitlement contains the three approved Apple subscriptions and matches the backend identifier.
- Created Apple consumables with US base prices and English (U.S.) localizations; all countries/regions selected and saved:
  - `mogging.evaluation`: $4.99, Apple ID `6812368366`, RevenueCat ID `prod9c5e8a77b5`.
  - `mogging.evaluation.pack3`: $9.99, Apple ID `6812371227`, RevenueCat ID `prodb12ac519f7`.
- Imported both products directly from App Store Connect into RevenueCat. Consumables have no Pro entitlement; the backend owns scan credits. The app fetches these products directly rather than through an offering.
- Configured the authorized RevenueCat webhook `whintgrca4689babf` for the Apple app, both environments, all events, with matching bearer authentication in production. Redeployed the existing production release to activate its environment variable.
- RevenueCat dashboard test delivery returned HTTP 200 with `{"data":{"received":true}}`. This proves authenticated delivery, not a real Apple purchase or credit grant.
- Uploaded the real simulator top-up paywall screenshot to both Apple consumables. Both images completed processing and persisted after reloading their product pages.
- Reproduced and fixed a purchase blocker: `flushAttribution()` threw `AttributionEngine must be bootstrapped before use` when initialization was unavailable. Session and purchase callers now safely skip flushing until attribution is ready, consistent with the other runtime guards. Both subscriptions and consumables use this function.
- After Fast Refresh, retrying the single-scan purchase reached Apple's account sign-in prompt instead of the attribution error. No transaction was confirmed.
- All 27 mobile tests pass, including the new pre-bootstrap/failed-Keychain regression test; TypeScript check passes.

## Blockers

- New consumables remain Prepare for Submission in Apple. Neither product has been submitted for review; Apple requires the first consumable submission with a new app version. RevenueCat initially reported Missing Metadata before screenshot upload.
- A Sandbox tester login is required to finish transactions. The simulator is currently at Apple's sign-in prompt. User asked to sign in there without sending credentials in chat. Actual subscription purchase, consumable crediting, and restore remain unverified end-to-end.
- Native automation clicks fail with `noWindowsAvailable`; the supported `serve-sim` browser mirror works and was used to retry the purchase.

## Still required

- Real Apple sandbox/TestFlight purchases for subscriptions and both consumables, cancellation, restore, relaunch persistence, backend access and credits, renewal/expiration/refund webhook delivery.
- Verify actual localized Apple prices and subscription terms.

## Re-run

From moggingnew (uses DATABASE_URL; temporary isolated schema removed at completion):

    bun run scripts/tests/revenuecat-credits.ts

Production preflight (creates/removes only a disposable account; no purchase):

    RUN_PURCHASE_LIVE_TEST=1 bun run scripts/tests/revenuecat-live.ts
